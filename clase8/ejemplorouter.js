// la estructura basica es parecida a la de un servidor con express

import { Router } from "express";
const router = Router();
router.get("/", (req, res) => {
	//cuerpo del servicio
});

export default router;

